# Architecture

The canonical architecture document is now [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

This root file previously held the origin quantum application's architecture notes
(the "Sol" quantum-peer tutor) and is superseded. See `docs/ARCHITECTURE.md` for the
framework as it stands, `docs/PRIVACY.md` for the privacy posture, and
`docs/PROVENANCE.md` for the origin record.
